apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-all-ingress
  namespace: cityflow
  labels:
    app.kubernetes.io/part-of: cityflow
spec:
  podSelector: {}
  policyTypes:
    - Ingress
