apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-backend-ingress
  namespace: cityflow
  labels:
    app.kubernetes.io/part-of: cityflow
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: backend
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: frontend
      ports:
        - port: 5000
          protocol: TCP
    - ports:
        - port: 5000
          protocol: TCP
  egress:
    # Allow backend to call Discord bot API
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: discord-bot
      ports:
        - protocol: TCP
          port: 5001
